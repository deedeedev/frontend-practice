import BlogPreviewCard from "@/components/BlogPreviewCard/BlogPreviewCard";

export default function Page() {
  return (
    <main className="w-full min-h-screen flex justify-center items-center bg-bpcYellow">
      <BlogPreviewCard />
    </main>
  );
}
